let soma = (num1, num2) => num1 + num2 // Quando tem apenas uma linha não precisa colocar chaves

let ola = (nome) => {
  `Hello: ${nome}`
}

let sayHello = name => `Hello ${name}` // Quando tem apenas 1 parãmetro não precisa colocar parênteses

console.log(sayHello('Igor'))